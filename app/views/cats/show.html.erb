<h1> <%= "#{@cat.name}" %> </h1>

<table>
  <tr>
    <th>
      Birth Date
    </th>
    <th>
      Color
    </th>
    <th>
      Sex
    </th>
  </tr>
  <tr>
    <td>
      <%= "#{@cat.birth_date}" %>
    </td>
    <td>
      <%= "#{@cat.color}" %>
    </td>
    <td>
      <%= "#{@cat.sex}" %>
    </td>
  </tr>
</table>

<h2>Description</h2>
<p>
  <%= "#{@cat.description}" %>
</p>
